# 测试者安装说明 —— v0.3.1

`v0.3.1` 现已合并进 `main`；原功能支线仍保留作为实现历史。

如果简历上传失败，最常见的原因是加载了错误的扩展目录。

正确安装方式：

1. 打开 `chrome://extensions/`
2. 开启“开发者模式”
3. 点击“加载已解压的扩展程序”
4. 选择构建后的 `dist/` 目录，或者 GitHub release 解压后的扩展目录

请不要直接加载仓库源码根目录。

如果加载的是源码根目录而不是 `dist/`，扩展界面可能仍然可以打开，但 `PDF` 或 `DOCX` 简历上传会因为解析文件缺失而失败。

## 更新现有安装

为了保留从 v0.1.2 开始的数据，不要把新 ZIP 解压后作为另一个目录加载：

1. 在 `chrome://extensions/` 打开现有扩展的“详细信息”，记下 `位置（Location）`。
2. 把新的 ZIP 解压到临时目录。
3. 替换原 `Location` 内的文件，但保持这个目录路径不变。
4. 在原扩展卡片上点击“重新加载（Reload）”。

用“加载已解压的扩展程序”加载新目录会生成不同的扩展 ID 和独立的本地存储。只要保留原目录和原 ID，本版本会保留原有配置、简历、收藏/手动岗位以及 v0.1.2 以来的缓存快照；之前没有分析的岗位不会被错误变成已分析。

## v0.3.1 测试重点

1. 打开侧边栏的 `Settings`，找到 `Title signals`。
2. 确认四种默认标题信号都已勾选。
3. 选择 `Color-blind friendly` 并保存，确认标题胶囊颜色发生变化。
4. 选择 `Custom colors`，为每种胶囊输入不同色号并保存，确认每种胶囊保持自己的颜色。
5. 确认“在岗位标题旁显示匹配关键词”默认未勾选。添加最多 5 个 JD 关键词，开启开关后分别测试 `Tag`、`Bracket` 和 `Spark` 三种标记样式。
6. 依次选择 OpenAI、Anthropic 和 Gemini，确认固定起始模型自动写入可编辑的 `Saved models`，再手动添加一个新模型并保存。
7. 在经典版 LinkedIn Jobs 页面测试；如果当前是 AI 搜索界面，请先切回经典版，然后刷新页面。
