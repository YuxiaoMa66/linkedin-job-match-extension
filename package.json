{
  "name": "linkedin-plugin",
  "version": "0.1.2",
  "description": "Chrome extension for LinkedIn resume-to-job matching with cached scoring and Netherlands sponsorship checks.",
  "main": "index.js",
  "scripts": {
    "setup": "node setup_public.js",
    "build": "npm run setup && vite build",
    "dev": "npm run build -- --watch"
  },
  "keywords": [
    "chrome-extension",
    "linkedin",
    "resume",
    "job-match",
    "gemini",
    "openai"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "mammoth": "^1.12.0",
    "pdfjs-dist": "^5.6.205"
  }
}
